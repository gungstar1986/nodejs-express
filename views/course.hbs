<div class="course">
    <h1>{{course.title}}</h1>
    <img src="{{course.url}}" alt="{{course.title}}">
    <p class="price big">{{course.price}}</p>
    <a href="/courses">Назад к выбору курса</a>
</div>
